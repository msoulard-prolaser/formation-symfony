{% extends 'base.html.twig' %}

{% block title 'Movie form' %}
    {% block body %}
        <h1>{{ movie ? 'Edit ' ~ movie.title  : 'New Movie'}}</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Send</button>
        {{ form_end(form) }}
    {% endblock %}