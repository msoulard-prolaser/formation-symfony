{% from 'includes/_macros.html.twig' import nav_link %}

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_default_index') }}">Sensio TV+</a>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            {{ nav_link(app.current_route, 'app_default_index', 'Home') }}
            {{ nav_link(app.current_route, 'app_movie_index', 'Movie') }}
            {{ nav_link(app.current_route, 'app_book_index', 'Book') }}
            {{ nav_link(app.current_route, 'app_default_contact', 'Contact') }}
        </ul>
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
            {% if app.user %}
                {{ nav_link(app.current_route, 'app_security_logout', 'Logout') }}
            {% else %}
                {{ nav_link(app.current_route, 'app_security_login', 'Login') }}
            {% endif %}
        </ul>
    </div>
</nav>